<?xml version="1.0" encoding="UTF-8"?>
<manifest>
  <remote name="personal-remote" fetch="https://github.com/khimaros/" revision="11.0" />
  <remote name="seedvault-app" fetch="https://github.com/seedvault-app/" revision="master" />

  <!-- this custom repo - do not change path -->
  <project path="vendor/custom" name="ros-custom-config-repo" remote="personal-remote" />

  <!-- microg repo from rattlesnakeos -->
  <project path="platform/prebuilts/microg" name="microg" remote="rattlesnakeos" clone-depth="1" />

  <!-- seedvault -->
  <project path="external/seedvault" name="seedvault" remote="seedvault-app" />

	<!-- aurora store -->
	<!--
  <project path="prebuilts/aurorastore" name="ros-aurora-store" remote="personal-remote" clone-depth="1" revision="master" />
	-->
</manifest>
